# azure-ubuntu-vm-rdp-gui
This project demonstrates the deployment of an Ubuntu Virtual Machine on Microsoft Azure with secure remote access. GUI-based access is enabled using Remote Desktop Protocol (RDP), providing both terminal and desktop environment access to the VM.
